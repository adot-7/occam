# Model identifiers are configuration, not code. Verify rates before a live run.
worker_fast:
  provider: openai_compat
  base_url: https://api.tensormux.com/v1
  api_key: ${TENSORMUX_API_KEY}
  model: glm-4-7-flash
  in_per_m: 0.0
  out_per_m: 0.0
  grant_equiv_in_per_m: 0.06
  grant_equiv_out_per_m: 0.40
  rpm: 12
  max_tokens: 8192
  supports_json_schema: false
  tool_choice_modes: [auto]
worker_alt:
  provider: openai_compat
  base_url: https://api.openai.com/v1
  api_key: ${OPENAI_API_KEY}
  model: gpt-5-nano
  in_per_m: 0.05
  out_per_m: 0.40
  rpm: 500
architect:
  provider: anthropic
  api_key: ${ANTHROPIC_API_KEY}
  model: claude-haiku-4-5-20251001
  in_per_m: 1.00
  out_per_m: 5.00
